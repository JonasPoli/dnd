<!DOCTYPE html>
<html lang="pt-br">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>{% block title %}Nova Entidade{% endblock %}</title>
        
        <link rel="preconnect" href="https://fonts.googleapis.com">
        <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
        <link href="https://fonts.googleapis.com/css2?family=Montserrat:wght@400;500;600;700&family=Lato:wght@400;700&family=Plus+Jakarta+Sans:wght@400;500;600;700&display=swap" rel="stylesheet">

        {% block stylesheets %}
        {% endblock %}

        {% block javascripts %}
            {% block importmap %}{{ importmap('app') }}{% endblock %}
        {% endblock %}
    </head>
    <body class="min-h-screen flex flex-col font-lato bg-background text-foreground transition-colors duration-300">
        
        <header x-data="{ isMenuOpen: false }" class="sticky top-0 z-50 bg-white/80 dark:bg-slate-900/80 backdrop-blur-md border-b border-border">
            <nav class="container mx-auto px-4 h-20 flex items-center justify-between">
                <!-- Logo -->
                <a href="/" class="flex items-center space-x-2">
                    <div class="w-10 h-10 bg-primary rounded-lg flex items-center justify-center text-white font-bold text-xl">
                        N
                    </div>
                    <span class="font-montserrat font-bold text-xl tracking-tight hidden sm:block">NovaEntidade</span>
                </a>

                <!-- Desktop Menu -->
                <div class="hidden md:flex items-center space-x-8 font-medium">
                    <a href="/" class="hover:text-primary transition-colors">Home</a>
                    <a href="#" class="hover:text-primary transition-colors">Serviços</a>
                    <a href="#" class="hover:text-primary transition-colors">Especialidades</a>
                    <a href="#" class="hover:text-primary transition-colors">Médicos</a>
                    <a href="#" class="hover:text-primary transition-colors">Blog</a>
                    
                    <button onclick="window.toggleTheme()" class="p-2 rounded-full hover:bg-slate-100 dark:hover:bg-slate-800 transition-colors">
                        {{ ux_icon('lucide:sun', {class: 'w-5 h-5 block dark:hidden'}) }}
                        {{ ux_icon('lucide:moon', {class: 'w-5 h-5 hidden dark:block'}) }}
                    </button>

                    <a href="/login" class="bg-primary text-white px-6 py-2 rounded-full font-semibold hover:bg-primary/90 transition-all shadow-md hover:shadow-lg">
                        Login
                    </a>
                </div>

                <!-- Mobile Toggle -->
                <button @click="isMenuOpen = !isMenuOpen" class="md:hidden p-2">
                    {{ ux_icon('lucide:menu', {class: 'w-6 h-6'}) }}
                </button>
            </nav>

            <!-- Mobile Menu -->
            <div x-show="isMenuOpen" 
                 x-transition:enter="transition ease-out duration-200"
                 x-transition:enter-start="opacity-0 -translate-y-4"
                 x-transition:enter-end="opacity-100 translate-y-0"
                 x-transition:leave="transition ease-in duration-150"
                 x-transition:leave-start="opacity-100 translate-y-0"
                 x-transition:leave-end="opacity-0 -translate-y-4"
                 class="md:hidden absolute top-20 left-0 w-full bg-white dark:bg-slate-900 border-b border-border p-4 space-y-4 shadow-xl">
                <a href="/" class="block text-lg font-medium hover:text-primary">Home</a>
                <a href="#" class="block text-lg font-medium hover:text-primary">Serviços</a>
                <a href="#" class="block text-lg font-medium hover:text-primary">Especialidades</a>
                <a href="#" class="block text-lg font-medium hover:text-primary">Médicos</a>
                <a href="/login" class="block w-full text-center bg-primary text-white py-3 rounded-xl font-bold">Login</a>
                <div class="pt-4 border-t border-border flex justify-between items-center">
                    <span>Mudar Tema</span>
                    <button onclick="window.toggleTheme()" class="p-2 border border-border rounded-lg">
                        {{ ux_icon('lucide:sun', {class: 'w-5 h-5 block dark:hidden'}) }}
                        {{ ux_icon('lucide:moon', {class: 'w-5 h-5 hidden dark:block'}) }}
                    </button>
                </div>
            </div>
        </header>

        <main class="flex-grow">
            {% block body %}{% endblock %}
        </main>

        <footer class="bg-slate-50 dark:bg-slate-950 border-t border-border pt-16 pb-8">
            <div class="container mx-auto px-4 grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-12">
                <!-- Col 1: Sobre -->
                <div class="space-y-4">
                    <div class="flex items-center space-x-2">
                        <div class="w-8 h-8 bg-primary rounded flex items-center justify-center text-white font-bold">N</div>
                        <span class="font-montserrat font-bold text-lg">NovaEntidade</span>
                    </div>
                    <p class="text-medical-text text-sm leading-relaxed">
                        Referência em cuidado e dedicação à sua saúde. Nossa equipe está pronta para oferecer o melhor atendimento.
                    </p>
                    <div class="flex space-x-4">
                        <a href="#" class="text-slate-400 hover:text-primary transition-colors">{{ ux_icon('lucide:instagram', {class: 'w-5 h-5'}) }}</a>
                        <a href="#" class="text-slate-400 hover:text-primary transition-colors">{{ ux_icon('lucide:facebook', {class: 'w-5 h-5'}) }}</a>
                        <a href="#" class="text-slate-400 hover:text-primary transition-colors">{{ ux_icon('lucide:linkedin', {class: 'w-5 h-5'}) }}</a>
                    </div>
                </div>

                <!-- Col 2: Links Rápidos -->
                <div>
                    <h4 class="font-montserrat font-bold text-slate-900 dark:text-white mb-6">Links Rápidos</h4>
                    <ul class="space-y-3 text-sm text-medical-text">
                        <li><a href="#" class="hover:text-primary transition-colors">Sobre Nós</a></li>
                        <li><a href="#" class="hover:text-primary transition-colors">Nossa Equipe</a></li>
                        <li><a href="#" class="hover:text-primary transition-colors">Carreiras</a></li>
                        <li><a href="#" class="hover:text-primary transition-colors">Contato</a></li>
                    </ul>
                </div>

                <!-- Col 3: Especialidades -->
                <div>
                    <h4 class="font-montserrat font-bold text-slate-900 dark:text-white mb-6">Especialidades</h4>
                    <ul class="space-y-3 text-sm text-medical-text">
                        <li><a href="#" class="hover:text-primary transition-colors">Cardiologia</a></li>
                        <li><a href="#" class="hover:text-primary transition-colors">Pediatria</a></li>
                        <li><a href="#" class="hover:text-primary transition-colors">Ortopedia</a></li>
                        <li><a href="#" class="hover:text-primary transition-colors">Dermatologia</a></li>
                    </ul>
                </div>

                <!-- Col 4: Horário -->
                <div>
                    <h4 class="font-montserrat font-bold text-slate-900 dark:text-white mb-6">Atendimento</h4>
                    <div class="space-y-4 text-sm text-medical-text">
                        <div class="flex justify-between border-b border-border pb-2">
                            <span>Seg - Sex</span>
                            <span class="font-semibold">08:00 - 18:00</span>
                        </div>
                        <div class="flex justify-between border-b border-border pb-2">
                            <span>Sábado</span>
                            <span class="font-semibold">08:00 - 12:00</span>
                        </div>
                        <div class="flex justify-between">
                            <span>Domingo</span>
                            <span class="text-red-500 font-semibold">Fechado</span>
                        </div>
                    </div>
                </div>
            </div>
            
            <div class="container mx-auto px-4 mt-16 pt-8 border-t border-border flex flex-col md:flex-row justify-between items-center text-xs text-slate-400">
                <p>&copy; 2025 NovaEntidade. Todos os direitos reservados.</p>
                <div class="flex space-x-6 mt-4 md:mt-0">
                    <a href="#" class="hover:text-primary">Políticas de Privacidade</a>
                    <a href="#" class="hover:text-primary">Termos de Uso</a>
                </div>
            </div>
        </footer>
    </body>
</html>
