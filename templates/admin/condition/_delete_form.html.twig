<form method="post" action="{{ path('admin_condition_delete', {'id': condition.id}) }}" onsubmit="return confirm('Tem certeza que deseja excluir esta condição?');" class="inline-block">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ condition.id) }}">
    <button class="p-2 bg-red-50 dark:bg-red-900/20 text-red-500 hover:bg-red-100 dark:hover:bg-red-900/30 rounded-xl transition-colors">
        {{ ux_icon('lucide:trash-2', {class: 'w-5 h-5'}) }}
    </button>
</form>
