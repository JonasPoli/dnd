{% extends 'base_admin.html.twig' %}

{% block title %}Dashboard | Admin{% endblock %}
{% block header_title %}Painel de Controle{% endblock %}

{% block body %}
    <!-- Welcome Section -->
    <div class="mb-10">
        <h1 class="text-3xl font-bold font-jakarta mb-2">Olá, Administrador! 👋</h1>
        <p class="text-slate-500 font-medium">Veja o que está acontecendo na NovaEntidade hoje.</p>
    </div>

    <!-- Stats Grid (Placeholder/Static for now) -->
    <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-2 gap-6 mb-12">
        <!-- Card 1 -->
        <div class="glass-panel p-6 rounded-3xl bg-white dark:bg-slate-900 border border-slate-200 dark:border-slate-800 shadow-sm hover:shadow-xl transition-all group">
            <div class="flex items-center justify-between mb-4">
                <div class="w-12 h-12 bg-orange-100 dark:bg-orange-900/30 rounded-2xl flex items-center justify-center text-orange-600 group-hover:scale-110 transition-transform">
                    {{ ux_icon('lucide:message-square', {class: 'w-6 h-6'}) }}
                </div>
            </div>
            <h3 class="text-slate-400 text-xs font-bold uppercase tracking-wider mb-1">Mensagens</h3>
            <p class="text-3xl font-bold">0</p>
            <a href="#" class="mt-4 flex items-center text-xs font-bold text-primary hover:underline">
                Ler mensagens {{ ux_icon('lucide:arrow-right', {class: 'w-3 h-3 ml-1'}) }}
            </a>
        </div>

        <!-- Card 2 -->
        <div class="glass-panel p-6 rounded-3xl bg-white dark:bg-slate-900 border border-slate-200 dark:border-slate-800 shadow-sm hover:shadow-xl transition-all group">
            <div class="flex items-center justify-between mb-4">
                <div class="w-12 h-12 bg-emerald-100 dark:bg-emerald-900/30 rounded-2xl flex items-center justify-center text-emerald-600 group-hover:scale-110 transition-transform">
                    {{ ux_icon('lucide:users', {class: 'w-6 h-6'}) }}
                </div>
            </div>
            <h3 class="text-slate-400 text-xs font-bold uppercase tracking-wider mb-1">Usuários</h3>
            <p class="text-3xl font-bold">1</p>
            <a href="#" class="mt-4 flex items-center text-xs font-bold text-primary hover:underline">
                Ver usuários {{ ux_icon('lucide:arrow-right', {class: 'w-3 h-3 ml-1'}) }}
            </a>
        </div>
    </div>
{% endblock %}
