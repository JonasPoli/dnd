{% extends 'base_admin.html.twig' %}

{% block header_title %}Nova Magia{% endblock %}

{% block body %}
<div class="mb-8">
    <a href="{{ path('admin_spell_index') }}" class="inline-flex items-center text-sm font-bold text-slate-400 hover:text-primary transition-colors mb-4">
        {{ ux_icon('lucide:arrow-left', {class: 'w-4 h-4 mr-2'}) }}
        Voltar para a lista
    </a>
    <h1 class="text-2xl font-bold dark:text-white">Cadastrar Nova Magia</h1>
    <p class="text-slate-500 text-sm">Adicione uma nova magia ao grimório do sistema.</p>
</div>

<div class="bg-white dark:bg-slate-900 rounded-3xl border border-border p-8 shadow-sm max-w-4xl">
    {{ include('admin/spell/_form.html.twig', {'button_label': 'Criar Magia'}) }}
</div>
{% endblock %}
