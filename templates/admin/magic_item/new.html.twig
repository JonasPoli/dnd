{% extends 'base_admin.html.twig' %}

{% block header_title %}Novo Item Mágico{% endblock %}

{% block body %}
<div class="max-w-4xl mx-auto">
    <div class="mb-8">
        <a href="{{ path('admin_magic_item_index') }}" class="text-slate-400 hover:text-primary transition-colors flex items-center space-x-2 text-sm font-bold uppercase tracking-widest">
            {{ ux_icon('lucide:arrow-left', {class: 'w-4 h-4'}) }}
            <span>Voltar para Lista</span>
        </a>
        <h1 class="text-3xl font-bold mt-4 dark:text-white">Adicionar Artefatos</h1>
        <p class="text-slate-500">Documente as propriedades e encantos do novo item.</p>
    </div>

    <div class="bg-white dark:bg-slate-900 rounded-3xl border border-border p-8 shadow-sm">
        {{ include('admin/magic_item/_form.html.twig') }}
    </div>
</div>
{% endblock %}
